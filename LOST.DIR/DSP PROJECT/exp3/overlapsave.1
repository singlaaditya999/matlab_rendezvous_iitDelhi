
clc
clear all
close all
x=input('enter the first sequence');
h=input('enter the second sequence');
n1=length(x);
n2=length(h);
N=max(n1,n2);
h1=[h zeros(1,N-n2)];
x=[x zeros(1,N-n1)];
H=zeros(N,N); %creating H matrix
H(1:n2,1)=h; %placing elements in 1st col
for j=1:N-1
    for i=1:N-1
        H(i+1,j+1)=H(i,j); % lower triangular matrix completed
    end
    H(1,j+1)=H(N,j); % completing remaining matrix
end
Y=(H*x')';
subplot(3,1,1)
stem(x)
xlabel('n-------->')
ylabel('x value')
title('entered sequence x')
subplot(3,1,2)
stem(h)
xlabel('n-------->')
ylabel('h value')
title('entered sequence h')
subplot(3,1,3)
stem(Y)
xlabel('n-------->')
ylabel('Y value')
title('covolution of Y' )
x1=x(1:3);
x2=x(5:8);
x3=x(9:12);


 