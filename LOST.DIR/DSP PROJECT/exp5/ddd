close all
clr
wc=input('enter the cut off frequency')
N=input('enter the no of sequence')
x(n)=randn(1,128)
for n=-((N-1)/2):((N-1)/2)
    if(n~=0)
    h(n)=(sin*wc*n)/(pi*n);
    else
      h(n)=wc/pi;
    end
    hold on
end
 h